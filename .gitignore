# Secrets — never commit
.env
.env.bak
*.key

# Local runtime artifacts (data lives in named Docker volumes, not here)
n8n-data/
ollama-data/
postgres-data/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
